yfinance>=0.2.28
pandas>=2.1.0
numpy>=1.24.0
python-dotenv>=1.0.0
newsapi-python>=0.2.7
scikit-learn>=1.3.0
scipy>=1.11.0
requests>=2.31.0  # For GitHub API
PyNaCl>=1.5.0  # For GitHub Secrets encryption
pytest>=7.4.0  # Test suite (offline regression net)

# tensorflow moved to requirements-ml.txt: it is ~500MB, was reinstalled on
# every CI run, and nothing imports ml_predictor (which degrades gracefully
# without it). Install with: pip install -r requirements-ml.txt
